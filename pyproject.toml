[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "Linear_Boussinesq"
version = "0.1"
dependencies = ["numpy", "firedrake", "matplotlib"]

[tool.hatch.build.targets.wheel]
packages = ["Linear_Boussinesq"]